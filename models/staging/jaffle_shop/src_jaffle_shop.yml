version: 2

sources:
  - name: jaffle_shop
    database: snowflake_sample_data
    schema: tpch_sf10
    tables:
      - name: customer
        columns:
          - name: C_CUSTKEY
            tests:
              - unique
              - not_null
        freshness: null
      - name: orders
        columns:
          - name: O_ORDERKEY
            tests:
              - unique
              - not_null      
        freshness: null
      - name: LINEITEM
        # freshness: 
        #   error_after:
        #     count: 50
        #     period: year
        #   warn_after:
        #     count: 40
        #     period: year
        # loaded_at_field: i_receipt_date